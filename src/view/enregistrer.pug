include partials/head.pug
include partials/menu.pug

div.titre3Galerie.d-flex.my-4
    h1.texteFocus1
        | Pret a rejoindre la commu ?
    br

if $error
    div.error-message
        p #{$error}

div
  p.textInfo
    | Inscris-toi et deviens l'artiste d'œuvres invisibles ! Remplis le formulaire ci-dessous et ajoute ta touche unique à notre collection.
div.container-fluid
  form#formulaireEnregistrement.formulaireEnregistrement(action="/glimpsGoneV2/enregistrer" method="POST" enctype="multipart/form-data")
    div.row.flex-md-row
      div.col-md-6.col-12
        div.mb-3
          label.label-custom(for="inputPrenom") Prénom:
          input.form-control#inputPrenom(name="prenom" placeholder="Prénom..." required)
        div.mb-3
          label.label-custom(for="inputNom") Nom:
          input.form-control#inputNom(name="nom" placeholder="Nom..." required)
        div.mb-3
          label.label-custom(for="inputEmail") Email:
          input.form-control#inputEmail(name="email" type="email" placeholder="Email..." required)
        div.mb-3
          label.label-custom(for="inputPassword") Mot de passe:
          input.form-control#inputPassword(name="password" type="password" placeholder="Mot de passe..." required)
      div.col-md-6.col-12
        div.mb-3
          label.label-custom(for="inputTelephone") Téléphone:
          input.form-control#inputTelephone(name="telephone" type="text" placeholder="Téléphone..." required)
        div.mb-3
          label.label-custom(for="inputBio") Bio:
          textarea.form-control#inputBio(name="bio" rows="4" placeholder="Parle-nous de toi..." required)
        div.mb-3
          label.label-custom(for="inputPhoto") Photo de profil:
          input.form-control#inputPhoto(name="photo" type="file" accept="image/*" required)
        button#boutonValiderEnregistrement.custom-bouton1(type="submit")
            span S'enregistrer

include partials/footer.pug
script(src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.0/gsap.min.js")
script(src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.0/ScrollTrigger.min.js")
script(src="https://unpkg.com/splitting/dist/splitting.min.js")
script(src="https://unpkg.com/lenis@1.0.45/dist/lenis.min.js")
script(src="/glimpsGoneV2/public/js/script.js")
